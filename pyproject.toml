[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "asr-evo"
version = "0.1.0"
description = "A desktop dictation assistant with ASR and LLM post-processing."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
  "openai>=1.84",
  "pydantic>=2.7",
  "pydantic-settings>=2.2",
  "python-dotenv>=1.0",
  "sounddevice>=0.4.6",
  "soundfile>=0.12.1",
  "tomli-w>=1.2",
  "pyobjc-framework-Cocoa>=10.3; sys_platform == 'darwin'",
  "pyobjc-framework-ApplicationServices>=10.3; sys_platform == 'darwin'",
  "pyobjc-framework-AVFoundation>=10.3; sys_platform == 'darwin'",
  "pyobjc-framework-Quartz>=10.3; sys_platform == 'darwin'",
  "pillow>=10.0; sys_platform == 'win32'",
  "pynput>=1.7.7; sys_platform == 'win32'",
  "pystray>=0.19.5; sys_platform == 'win32'",
  "pywin32>=306; sys_platform == 'win32'",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2",
  "pytest-asyncio>=0.23",
  "ruff>=0.5",
]

[project.scripts]
asr-evo = "asr_evo.app:main"
asr-evo-control = "asr_evo.cli.control:main"
asr-evo-transcribe = "asr_evo.cli.transcribe_file:main"
asr-evo-insert-test = "asr_evo.cli.insert_test:main"

[project.gui-scripts]
asr-evow = "asr_evo.app:main"

[tool.setuptools.packages.find]
include = ["asr_evo*"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
  "integration: tests that call external services and require explicit environment variables",
]
